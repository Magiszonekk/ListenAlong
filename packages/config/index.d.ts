declare const config: {
  search: {
    odesliBonus: number;
    titleBonus: number;
    scriptBonus: number;
    ytSearchCount: number;
  };
  polling: {
    spotifyMs: number;
    driftFactor: number;
    driftBaseMs: number;
  };
};

export = config;
