declare const config: {
  search: {
    titleBonus: number;
    scriptBonus: number;
    ytSearchCount: number;
  };
  polling: {
    spotifyMs: number;
    driftFactor: number;
    driftBaseMs: number;
  };
};

export = config;
