declare const config: {
  search: {
    odesliBonus: number;
    titleBonus: number;
    scriptBonus: number;
    ytSearchCount: number;
  };
  polling: {
    spotifyMs: number;
    listenersMs: number;
  };
};

export = config;
